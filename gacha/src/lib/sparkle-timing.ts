/** Original SMIL flash length in the star SVGs. */
export const FLASH_S = 1.04

/** Full loop: flash, then idle so capsules do not shimmer. */
export const SPARKLE_CYCLE_S = 4.2

export type SparkleVariant = 'a' | 'b' | 'c'
